version: '3.8'

services:
  # The Backend Service
  backend:
    build: ./server
    ports:
      - "8000:8000"  

  # The Frontend Service
  frontend:
    build: ./client
    ports:
      - "3000:5173" 